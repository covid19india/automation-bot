class PreProcessor():
    def self.process_links(response):

    def process_photo(response):

    def process_file(response):
        # Files will be present in tmp

    def get_twitter_images(tweet_url):
        '''
        Authenticate twitter API and
        download all the images in tweet to tmp folder
        '''